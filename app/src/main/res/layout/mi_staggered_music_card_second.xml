<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    app:cardBackgroundColor="@android:color/transparent"
    app:cardElevation="0dp"
    app:cardPreventCornerOverlap="true"
    app:contentPaddingTop="@dimen/mi_staggered_music_grid_margin_top_second">

    <LinearLayout
        android:layout_width="@dimen/mi_staggered_music_grid_card_width_landscape"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/mi_staggered_music_grid_spacing_small"
        android:layout_marginRight="@dimen/mi_staggered_music_grid_spacing_small"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/music_image"
            android:layout_width="match_parent"
            android:layout_height="@dimen/mi_staggered_music_grid_card_height_landscape"
            android:background="?attr/colorPrimaryDark"
            android:scaleType="centerCrop" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:id="@+id/music_title"
                android:text=""
                style="@style/TextAppearance.MyItunes.MusicTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textAlignment="center"/>

        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
